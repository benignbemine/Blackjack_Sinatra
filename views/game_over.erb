<h4>Your total is <%= calculate_total(session[:player_cards]) %>.
The dealers total is <%= calculate_total(session[:dealer_cards])%>.
</4>
<h1>
<% if bust(session[:dealer_cards])%>
  Dealer busted!!! You win $<%=session[:bet]%> !!!!
  <% session[:player_wallet]+=session[:bet] %>
<% elsif bust(session[:player_cards])%>
  You busted!!!! You lose $<%=session[:bet]%>!!!
  <% session[:player_wallet]-=session[:bet] %>
<% elsif calculate_total(session[:player_cards])>calculate_total(session[:dealer_cards]) %>
  You win $<%=session[:bet]%>!!!
  <% session[:player_wallet]+=session[:bet] %>
<% else %>
  You Lose $<%=session[:bet]%>!!!
  <% session[:player_wallet]-=session[:bet] %>
<% end %>
</h1>
<h2>Would you like to play again?</h2>
<form action='/game_over' method='post'>
<input type='submit' name='replay' value='Yes' class="btn">
<input type='submit' name='replay' value='No' class = "btn">
</form>